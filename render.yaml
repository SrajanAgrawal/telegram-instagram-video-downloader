services:
  - type: worker
    name: telegram-instagram-bot
    runtime: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python bot.py"
    envVars:
      - key: TELEGRAM_BOT_TOKEN
        value: your_token_here  # Or set this in the Render dashboard instead